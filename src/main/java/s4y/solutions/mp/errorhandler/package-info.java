/**
 * Contains the classes for the error handling.
 *
 * <p>In the project, an approach is used where exceptions are not thrown from methods,
 * but are instead forwarded to a global handler, which returns a value that signals an error in order to avoid
 * try-catch blocks.</p>
 */

package s4y.solutions.mp.errorhandler;
